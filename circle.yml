dependencies:
    override:
        - nvm install 8.9.3
        - nvm install 6.12.2

test:
    override:
        - nvm use 6.12.2
        - node -v && npm -v
        - npm install -g nodeunit grunt-cli
        - npm install
        - grunt build
        - npm test
        
        - rm -r node_modules
        
        - nvm use 8.9.3
        - node -v && npm -v
        - npm install -g nodeunit grunt-cli
        - npm install
        - grunt build
        - npm test
