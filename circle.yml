machine:
    node:
        version:
            4.4.1

dependencies:
    override:
        - npm install -g nodeunit
        - npm install -g grunt-cli
        - npm install

test:
    override:
        - grunt build
        - npm test
