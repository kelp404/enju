machine:
    node:
        version:
            6.11.0

dependencies:
    override:
        - npm install -g nodeunit
        - npm install -g grunt-cli
        - npm install

test:
    override:
        - grunt build
        - npm test
